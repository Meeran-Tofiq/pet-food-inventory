extends layout 

block content 
  div.delete-page.with-background
    h1= title
    div.delete-card
      p #[strong Are you sure you want to delete this pet food?]

      div.form-container
        form(method='POST')
          input(type='hidden', name='pet_food_id', value=petFood._id )

          button(type='submit') Delete